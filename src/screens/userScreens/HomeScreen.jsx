// Bridge export so existing imports (`../screens/userScreens/HomeScreen`)
// keep working while we host the actual screen in jobSeekerScreens.
import HomeScreen from "./jobSeekerScreens/HomeScreen";

export default HomeScreen;


